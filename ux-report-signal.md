# Signal AI 行业日报 — 用户体验痛点报告

> 测试日期：2026-07-16 | 工具：Chrome MCP (HTTP API) + GLM-4.6V Flash 视觉模型
> 测试站点：http://1y4w1s.icu:8080/

---

## 🔴 严重痛点

### 1. [高] 引导弹窗遮挡核心内容
- **问题**：首次进入时全屏模态弹窗"欢迎使用 Signal"覆盖整个页面，必须点击"开始"才能看到日报内容。弹窗中的描述性文字过多（"每天5分钟的AI行业脉搏"、"从全球AI新闻里挑出最值得读的，配上一句话观点"），新用户无法快速 skip。
- **建议**：改为轻量级 toast/banner 提示，或允许直接点击背景关闭；减少弹窗文字量。

### 2. [高] 搜索功能交互路径不直观
- **问题**：搜索框在右上角显示为"搜索… ⌘K"，点击后展开输入框。但输入关键词（如"OpenAI"）后按 Enter 并未触发搜索跳转或结果更新——搜索似乎是前端过滤，但过滤逻辑不明确（页面内容未变化）。
- **建议**：明确搜索交互模式：要么实时过滤（输入即时显示结果），要么跳转到搜索结果页面。添加搜索状态指示和空结果状态提示。

### 3. [高] "今日日报" +12 徽标无引导
- **问题**：侧边栏"今日日报"显示 "+12" 新文章徽标，但没有进一步提示用户这些是新内容。点击后页面似乎无变化（相同 URL），让用户困惑。
- **建议**：点击"今日日报"时应有视觉反馈（如内容滚动到最新文章、高亮新条目），或切换到专用日报视图。

---

## 🟡 中等痛点

### 4. [中] AI 助手浮窗与主内容重叠
- **问题**：右下角的 AI 助手聊天浮窗（pe=none 表示隐藏/不可交互？）与日报内容区域有重叠，尤其是在窄屏或缩放时。关闭搜索按钮 (`×`) 也在这个区域，容易误触。
- **建议**：AI 助手采用可收起/展开的浮动按钮，而非固定面板；为搜索关闭按钮增加安全边距。

### 5. [中] 侧边导航项目缺少当前状态指示
- **问题**：侧边栏有"今日日报"、"日报归档"、"我的收藏"、"浏览历史"等导航项。当前选中的项虽然有 `active` class，但视觉区分度不够（仅 text color 变化），用户可能不清楚当前位置。
- **建议**：添加背景色高亮或左侧指示条；确保 active 状态与 inactive 状态有足够对比度。

### 6. [中] 文章卡片信息层级不够清晰
- **问题**：每日速览中的文章条目混合了：标题、摘要、编辑部观点、原文链接、来源标签。信息密度高但视觉层级扁平，用户扫读困难。
- **建议**：使用卡片式设计区分不同文章；标题加粗放大；来源标签用颜色区分；"编辑部观点"用引用块样式突出。

### 7. [中] 事件聚类标签功能未充分展示
- **问题**：页面顶部有"重要性"、"来源"、"标签"三个筛选按钮，但点击后缺乏明确的筛选状态反馈和结果计数。用户不知道当前正在应用什么筛选条件。
- **建议**：选中的筛选按钮添加视觉状态（如填充色）；显示"筛选结果：X 条"；提供清除筛选的入口。

---

## 🟢 低优先级

### 8. [低] 缺少黑暗模式
- **问题**：目前仅支持浅色主题。对于 AI 行业用户（常夜间阅读），深色模式是刚需。
- **建议**：添加 dark mode 切换，基于 `prefers-color-scheme` 媒体查询自动适配。

### 9. [低] 响应式适配不足
- **问题**：页面布局固定宽度，在 1280×800 窗口下正常，但更小窗口会溢出或内容被截断。侧边栏在窄屏上可能不可用。
- **建议**：添加响应式断点，窄屏时侧边栏可折叠为汉堡菜单。

### 10. [低] 无障碍（A11y）问题
- **问题**：部分按钮缺少 aria-label；搜索框缺少明确的 label 关联；颜色对比度可能不足（"不再显示"为灰色文字）。
- **建议**：添加 ARIA 标签；确保色彩对比度达到 WCAG AA 标准。

---

## 总结

Signal 的核心功能（AI 行业日报聚合 + 事件聚类 + 编辑观点）非常有价值，内容质量高。主要体验瓶颈在于：
1. **首次使用引导**过于厚重
2. **搜索功能**交互不明确
3. **页面信息层级**可进一步优化
4. **响应式和无障碍**需要补齐

优先修复搜索功能和引导流程，其次是信息层级优化。
