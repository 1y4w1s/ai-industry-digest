# Signal AI 日报 — 服务器代码全集

> 仅收录部署在服务器上的代码文件，方便运维查阅
> 最后更新：2026-06-04

---

## 目录

1. [Nginx 配置](#1-nginx-配置)
2. [后端入口 — api/main.py](#2-后端口---apimainpy)
3. [后端认证路由 — api/routes/auth.py（含 UUID 修复）](#3-后端认证路由---apiroutesauthpy含-uuid-修复)
4. [后端数据库层 — api/models/database.py](#4-后端数据库层---apimodelsdatabasepy)
5. [前端 API 客户端 — frontend/src/api/client.js（含 Demo token）](#5-前端-api-客户端---frontendsrcapiclientjs含-demo-token)
6. [环境变量 — .env.example](#6-环境变量---envexample)
7. [前端环境变量 — frontend/.env](#7-前端环境变量---frontendenv)
8. [数据库建表 SQL — scripts/init_schema.sql](#8-数据库建表-sql---scriptsinit_schemasql)
9. [Python 依赖 — requirements.txt](#9-python-依赖---requirementstxt)
10. [全流程入口 — run.py](#10-全流程入口---runpy)
11. [Vite 配置 — frontend/vite.config.js](#11-vite-配置---frontendviteconfigjs)

---

## 1. Nginx 配置

**服务器路径**：`/etc/nginx/sites-available/default`

```nginx
server {
    listen 8080 default_server;
    listen [::]:8080 default_server;

    root /opt/ai-industry-digest/frontend/dist;
    index index.html;

    server_name _;
    include /etc/nginx/mime.types;

    # 前端路由支持（SPA 单页应用）
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 反向代理到 FastAPI 后端
    location /api/ {
        proxy_pass http://localhost:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态文件缓存（JS、CSS、图片）
    location ~* \.(js|css|png|jpg|svg|ico)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## 2. 后端入口 — api/main.py

**功能**：FastAPI 应用启动、CORS 配置、速率限制中间件、路由注册

```python
"""
Signal - FastAPI 入口
"""

import time
import os
from pathlib import Path

from fastapi import FastAPI, Request
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import JSONResponse
from fastapi.staticfiles import StaticFiles

from api.routes.content import router as content_router
from api.routes.auth import router as auth_router
from api.routes.chat import router as chat_router

app = FastAPI(
    title="Signal API",
    description="AI 行业日报系统后端接口",
    version="2.0.0",
)

# CORS 配置（允许所有来源）
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 速率限制中间件（每 IP 每分钟最多 30 次）
rate_limit_store = {}

@app.middleware("http")
async def rate_limit_middleware(request: Request, call_next):
    client_ip = request.client.host if request.client else "unknown"
    path = request.url.path

    if path.startswith("/test/") or path.startswith("/docs") \
       or path.startswith("/openapi") or path.startswith("/api/proxy"):
        return await call_next(request)

    now = time.time()
    window = 60
    max_requests = 30

    rate_limit_store[client_ip] = [
        t for t in rate_limit_store.get(client_ip, [])
        if now - t < window
    ]

    if len(rate_limit_store[client_ip]) >= max_requests:
        return JSONResponse(
            status_code=429,
            content={"detail": "请求过于频繁，请稍后再试", "retry_after": 60}
        )

    rate_limit_store[client_ip].append(now)
    return await call_next(request)


# 注册路由
app.include_router(content_router, prefix="/api")
app.include_router(auth_router, prefix="/api/auth")
app.include_router(chat_router, prefix="/api")

# 静态文件托管（测试前端）
test_dir = Path(__file__).resolve().parent.parent / "test"
if test_dir.exists():
    app.mount("/test", StaticFiles(directory=str(test_dir), html=True), name="test")


@app.get("/")
async def root():
    return {
        "name": "Signal",
        "version": "2.0.0",
        "docs": "/docs",
        "test_frontend": "/test/",
    }
```

## 3. 后端认证路由 — api/routes/auth.py（含 UUID 修复）

**功能**：用户认证、收藏、浏览历史、反馈接口。**核心修复**：将 `demo-user` 转换为有效 UUID。

```python
"""
Signal - 用户认证与个人数据接口
依赖 Supabase Auth，通过 Authorization Header 鉴权
"""

import os
import jwt
from fastapi import APIRouter, Depends, HTTPException, Header
from pydantic import BaseModel
from typing import Optional

from api.models.database import DatabaseManager

router = APIRouter()
db = DatabaseManager()


# Demo 用户配置（用于开发测试）
DEMO_USER_ID = "demo-user"
DEMO_USER_UUID = "00000000-0000-0000-0000-000000000001"

def get_user_id(authorization: str = Header(None)) -> str:
    if not authorization:
        raise HTTPException(status_code=401, detail="未登录")
    
    token = authorization.replace("Bearer ", "")
    
    if token == DEMO_USER_ID:
        return DEMO_USER_UUID
    
    try:
        decoded = jwt.decode(token, options={"verify_signature": False})
        user_id = decoded.get("sub")
        if user_id:
            return user_id
    except Exception:
        pass
    
    return token


class BookmarkRequest(BaseModel):
    article_id: str
    note: str = ""

class BookmarkDeleteRequest(BaseModel):
    bookmark_id: str

class HistoryRequest(BaseModel):
    article_id: str

class FeedbackRequest(BaseModel):
    article_id: str
    feedback: str  # thumbs_up / thumbs_down


@router.get("/me", tags=["用户"])
async def get_profile(user_id: str = Depends(get_user_id)):
    profile = db.get_or_create_profile(user_id)
    return profile


@router.post("/bookmarks", tags=["收藏"])
async def add_bookmark(req: BookmarkRequest, user_id: str = Depends(get_user_id)):
    return db.add_bookmark(user_id, req.article_id, req.note)

@router.delete("/bookmarks/{bookmark_id}", tags=["收藏"])
async def remove_bookmark(bookmark_id: str, user_id: str = Depends(get_user_id)):
    ok = db.remove_bookmark(bookmark_id, user_id)
    if not ok:
        raise HTTPException(status_code=404, detail="收藏不存在")
    return {"success": True}

@router.get("/bookmarks", tags=["收藏"])
async def list_bookmarks(page: int = 1, page_size: int = 20, user_id: str = Depends(get_user_id)):
    return db.get_bookmarks(user_id, page=page, page_size=page_size)


@router.post("/history", tags=["历史"])
async def add_history(req: HistoryRequest, user_id: str = Depends(get_user_id)):
    db.add_reading_history(user_id, req.article_id)
    return {"success": True}

@router.get("/history", tags=["历史"])
async def list_history(page: int = 1, page_size: int = 20, user_id: str = Depends(get_user_id)):
    return db.get_reading_history(user_id, page=page, page_size=page_size)


@router.post("/feedback", tags=["反馈"])
async def submit_feedback(req: FeedbackRequest, user_id: str = Depends(get_user_id)):
    try:
        return db.submit_feedback(user_id, req.article_id, req.feedback)
    except ValueError as e:
        raise HTTPException(status_code=400, detail=str(e))
```

## 4. 后端数据库层 — api/models/database.py

**功能**：Supabase 客户端封装，包含文章 CRUD、日报查询、用户收藏/历史/反馈操作。

```python
"""
Signal - 数据库管理
Supabase 写入、查询、分页、搜索
"""

import os
import time
from typing import List, Optional, Dict, Any
from datetime import datetime, date

from supabase import create_client, Client
from dotenv import load_dotenv

from collector.base import Article

load_dotenv()


class DatabaseManager:
    """Supabase 数据库管理器"""

    MAX_RETRIES = 3
    RETRY_DELAY = 2

    def __init__(self):
        url = os.getenv("SUPABASE_URL")
        key = os.getenv("SUPABASE_KEY")
        if not url or not key:
            raise ValueError("请设置环境变量 SUPABASE_URL 和 SUPABASE_KEY")
        self._url = url
        self._key = key
        self.client: Client = self._create_client()

    def _create_client(self) -> Client:
        return create_client(self._url, self._key)

    def _execute_with_retry(self, operation, *args, **kwargs):
        last_error = None
        for attempt in range(self.MAX_RETRIES + 1):
            try:
                return operation(*args, **kwargs)
            except Exception as e:
                last_error = e
                if attempt < self.MAX_RETRIES:
                    wait = self.RETRY_DELAY * (2 ** attempt)
                    print(f"  [DB RETRY] 操作失败，{wait}秒后重试 ({attempt + 1}/{self.MAX_RETRIES}): {e}")
                    time.sleep(wait)
                    self.client = self._create_client()
                else:
                    print(f"  [DB ERROR] 操作最终失败: {e}")
                    raise
        raise last_error

    # ── 写入 ──
    def save_articles(self, articles: List[Article]) -> dict:
        result = {"inserted": 0, "skipped": 0, "errors": 0}
        for article in articles:
            try:
                existing = self.client.table("articles").select("id").eq("url", article.url).execute()
                if existing.data and len(existing.data) > 0:
                    result["skipped"] += 1
                    continue
                data = {
                    "title": article.title,
                    "url": article.url,
                    "source_name": article.source_name,
                    "raw_content": article.raw_content[:50000],
                    "summary": article.summary or "",
                    "tags": article.tags or [],
                    "importance": article.importance or "low",
                    "importance_reason": article.importance_reason or "",
                    "source_refs": article.source_refs or [],
                    "published_at": article.published_at.isoformat() if article.published_at else None,
                }
                self.client.table("articles").insert(data).execute()
                result["inserted"] += 1
            except Exception as e:
                result["errors"] += 1
        return result

    # ── 查询 ──
    def get_articles(self, page=1, page_size=20, keyword=None, tag=None,
                     source=None, importance=None, date_from=None, date_to=None,
                     sort_by="published_at", sort_order="desc") -> Dict[str, Any]:
        query = self.client.table("articles").select("*", count="exact")
        if keyword:
            query = query.or_(f"search_vector.phfts.{keyword},title.ilike.%{keyword}%")
        if tag:
            query = query.contains("tags", [tag])
        if source:
            query = query.eq("source_name", source)
        if importance:
            query = query.eq("importance", importance)
        if date_from:
            query = query.gte("published_at", date_from)
        if date_to:
            query = query.lte("published_at", date_to)
        order_direction = sort_order if sort_order in ("asc", "desc") else "desc"
        query = query.order(sort_by, desc=(order_direction == "desc"))
        offset = (page - 1) * page_size
        query = query.range(offset, offset + page_size - 1)
        result = query.execute()
        total = result.count or 0
        return {
            "items": result.data, "total": total, "page": page,
            "page_size": page_size, "pages": (total + page_size - 1) // page_size if page_size > 0 else 0,
        }

    def get_article_by_id(self, article_id: str) -> Optional[dict]:
        result = self.client.table("articles").select("*").eq("id", article_id).execute()
        return result.data[0] if result.data else None

    def get_reports(self, page=1, page_size=7) -> Dict[str, Any]:
        query = self.client.table("daily_reports").select("*", count="exact").order("report_date", desc=True)
        offset = (page - 1) * page_size
        query = query.range(offset, offset + page_size - 1)
        result = query.execute()
        total = result.count or 0
        return {"items": result.data, "total": total, "page": page, "page_size": page_size,
                "pages": (total + page_size - 1) // page_size if page_size > 0 else 0}

    def get_report_by_date(self, report_date: str) -> Optional[dict]:
        result = self.client.table("daily_reports").select("*").eq("report_date", report_date).execute()
        if not result.data:
            return None
        report = result.data[0]
        article_ids = report.get("article_ids", [])
        if article_ids:
            raw = self.client.table("articles").select("*").in_("id", article_ids).execute()
            raw_articles = raw.data or []
            grouped = {"high": [], "medium": [], "low": []}
            for a in raw_articles:
                imp = a.get("importance", "low") or "low"
                if imp in grouped:
                    grouped[imp].append(a)
                else:
                    grouped["low"].append(a)
            report["articles"] = grouped
        else:
            report["articles"] = {"high": [], "medium": [], "low": []}
        return report

    def get_article_count(self) -> int:
        result = self.client.table("articles").select("id", count="exact").execute()
        return result.count or 0

    def get_sources(self) -> List[str]:
        result = self.client.table("articles").select("source_name").execute()
        sources = set()
        for row in result.data or []:
            if row.get("source_name"):
                sources.add(row["source_name"])
        return sorted(sources)

    def get_tags(self) -> List[str]:
        result = self.client.table("articles").select("tags").execute()
        tags = set()
        for row in result.data or []:
            for tag in row.get("tags", []) or []:
                tags.add(tag)
        return sorted(tags)

    # ── 用户 ──
    def get_or_create_profile(self, user_id, nickname=None, avatar_url=None) -> dict:
        result = self.client.table("user_profiles").select("*").eq("id", user_id).execute()
        if result.data:
            return result.data[0]
        data = {"id": user_id}
        if nickname: data["nickname"] = nickname
        if avatar_url: data["avatar_url"] = avatar_url
        self.client.table("user_profiles").insert(data).execute()
        return data

    def add_bookmark(self, user_id, article_id, note="") -> dict:
        result = self.client.table("bookmarks").insert({"user_id": user_id, "article_id": article_id, "note": note}).execute()
        return result.data[0] if result.data else {}

    def remove_bookmark(self, bookmark_id, user_id) -> bool:
        result = self.client.table("bookmarks").delete().eq("id", bookmark_id).eq("user_id", user_id).execute()
        return len(result.data) > 0

    def get_bookmarks(self, user_id, page=1, page_size=20) -> Dict[str, Any]:
        query = self.client.table("bookmarks").select("*, articles(*)", count="exact").eq("user_id", user_id).order("created_at", desc=True)
        offset = (page - 1) * page_size
        query = query.range(offset, offset + page_size - 1)
        result = query.execute()
        total = result.count or 0
        return {"items": result.data, "total": total, "page": page, "page_size": page_size,
                "pages": (total + page_size - 1) // page_size if page_size > 0 else 0}

    def add_reading_history(self, user_id, article_id) -> bool:
        today = date.today().isoformat()
        existing = self.client.table("reading_history").select("id").eq("user_id", user_id).eq("article_id", article_id).gte("read_at", today).execute()
        if existing.data:
            return False
        self.client.table("reading_history").insert({"user_id": user_id, "article_id": article_id}).execute()
        return True

    def get_reading_history(self, user_id, page=1, page_size=20) -> Dict[str, Any]:
        query = self.client.table("reading_history").select("*, articles(*)", count="exact").eq("user_id", user_id).order("read_at", desc=True)
        offset = (page - 1) * page_size
        query = query.range(offset, offset + page_size - 1)
        result = query.execute()
        total = result.count or 0
        return {"items": result.data, "total": total, "page": page, "page_size": page_size,
                "pages": (total + page_size - 1) // page_size if page_size > 0 else 0}
```

## 5. 前端 API 客户端 — frontend/src/api/client.js（含 Demo token）

```javascript
const API_BASE = '/api';
const TOKEN_KEY = 'signal_auth_token';
const DEMO_TOKEN = 'demo-user';

function getToken() {
  try { return localStorage.getItem(TOKEN_KEY); } catch { return null; }
}

async function request(path, options = {}) {
  const token = getToken() || DEMO_TOKEN;
  // ... headers, fetch, error handling
}
```

## 6. 环境变量 — .env.example

```
SUPABASE_URL=https://vobpkdrujixghvttgkuq.supabase.co
SUPABASE_KEY=
DEEPSEEK_API_KEY=
```

## 7. 前端环境变量 — frontend/.env

```
VITE_SUPABASE_URL=https://vobpkdrujixghvttgkuq.supabase.co
VITE_SUPABASE_ANON_KEY=
```

## 8. 数据库建表 SQL — scripts/init_schema.sql

包含 articles、daily_reports、user_profiles、bookmarks、reading_history、article_feedback 共 6 张表的建表语句。

## 9. Python 依赖 — requirements.txt

```
fastapi
uvicorn
supabase
python-dotenv
httpx
feedparser
beautifulsoup4
PyJWT
pytest
```

## 10. 全流程入口 — run.py

运行采集 → AI 处理 → 日报生成 → 知识库导入（可选）的完整流水线。

## 11. Vite 配置 — frontend/vite.config.js

```javascript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  server: { port: 5173 },
});
```
