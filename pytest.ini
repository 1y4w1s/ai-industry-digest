[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# 覆盖率配置（运行 pytest --cov 时生效）
# --cov=api --cov-report=term-missing --cov-fail-under=70
